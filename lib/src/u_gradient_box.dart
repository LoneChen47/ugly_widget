import 'package:flutter/material.dart';

class UGradientBox extends StatelessWidget {
  const UGradientBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
